# Token 用量统计实施计划

## 目标

从新版首次启动时开始，按账号统计当前 5 小时周期、当天和当前周的 Token 数量与 API 等值估算价格；不扫描旧记录，不显示已消耗 Credit。

## 数据边界

- 首次启用时把现有会话文件位置记为起点，旧内容不进入统计。
- 后续只读取 `~/.codex/sessions` 和 `~/.codex/archived_sessions` 新增内容。
- 每条记录保存账号、时间、模型、输入、缓存输入、缓存写入、输出和推理 Token。
- Credit 只展示用量接口返回的余额；余额为空时显示“未启用”。
- 价格使用记录中的模型和随应用发布的 OpenAI API 单价计算，并明确标为估算值。

## 实施步骤

- [x] 新增增量读取器、游标文件和账号切换时间记录。
- [x] 增加 Token 聚合与价格计算测试。
- [x] 在主界面增加“Token 统计”入口和详情窗口。
- [x] 在账号切换前处理旧账号新增记录，切换成功后记录新账号起点。
- [x] 在刷新和应用启动时执行毫秒级增量读取。
- [x] 补充中英文界面文案。
- [x] 构建本地应用并验证首次启用不导入历史数据。
- [x] 完成提交前检查并准备自动 Release。
